# Copy to .env.local for local development. Never commit the real file.

# Stream F — contact form delivery (api/contact.ts). Without this the
# endpoint returns 503 and the UI falls back to a mailto: link.
RESEND_API_KEY=
